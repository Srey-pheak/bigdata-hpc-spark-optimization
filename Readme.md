Overview
This project analyses and optimises a PySpark script that processes SLURM job accounting data from the Deucalion HPC cluster. The script reads monthly CSV files, computes aggregated statistics by compute partition type (ARM, AMD, GPU) and funding agency (FCT, EuroHPC, LOCAL), and produces a LaTeX output file (params.tex) for quarterly reporting.
